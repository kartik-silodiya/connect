/** @type {import('next').NextConfig} */
const nextConfig = {
  // Keep your existing config options here. 
  // If you had images or experimental options, paste them inside these brackets.
  images: {
    domains: ['localhost'], // Example: add any external image domains you are using
  },
};

export default nextConfig;